# ─────────────────────────────────────────────────────────────
# NoteKart environment variables
# Copy to .env.local and fill in. Never commit real secrets.
# ─────────────────────────────────────────────────────────────

# Database (Neon Postgres) — required
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Public site URL, used to build payment redirect URLs — required in production
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Session signing + OTP hashing — REQUIRED. Generate with:
#   openssl rand -base64 32
SESSION_SECRET=

# Comma-separated 10-digit admin mobile numbers that may access /admin
ADMIN_MOBILES=

# Public WhatsApp help button (10-digit Indian number or full country-code number)
NEXT_PUBLIC_WHATSAPP_NUMBER=9461217285

# ── OTP delivery (SMS) ──────────────────────────────────────────
# Pick and fully configure one live provider. OTP requests fail safely
# when no real SMS provider is available.
# SMS_PROVIDER=msg91   # or "twilio"; auto-detected from the keys below if unset
# SMS_COUNTRY_CODE=91

# MSG91 (default for India). Template variable should map to the OTP.
MSG91_AUTH_KEY=
MSG91_TEMPLATE_ID=
MSG91_SENDER_ID=

# MSG91 OTP Widget (recommended for this project)
# Public values used by the browser widget:
NEXT_PUBLIC_MSG91_WIDGET_ID=
NEXT_PUBLIC_MSG91_WIDGET_TOKEN=
# Server-side auth key used to verify the widget's access-token:
# MSG91_AUTH_KEY=

# Twilio (alternative)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ── Payments ────────────────────────────────────────────────────
# Admin can switch the active gateway in /admin. Default is Cashfree.
PAYMENT_GATEWAY=cashfree

# Cashfree PG. Live checkout stays unavailable until credentials are configured.
CASHFREE_ENV=production   # or "sandbox"
CASHFREE_APP_ID=
CASHFREE_SECRET_KEY=
CASHFREE_API_VERSION=2023-08-01

# PhonePe PG. Live checkout stays unavailable until credentials are configured.
PHONEPE_ENV=sandbox   # or "production"
PHONEPE_CLIENT_ID=
PHONEPE_CLIENT_SECRET=
PHONEPE_CLIENT_VERSION=1

# ── Media uploads (Cloudinary) ──────────────────────────────────
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_URL=

# ── Shipping / tracking ─────────────────────────────────────────
# These are the only Delhivery environment variables. Pickup location and
# parcel weight are managed from the admin Delivery Review page.
DELHIVERY_API_TOKEN=
DELHIVERY_ENV=production
DELHIVERY_SHIPPING_MODE=Surface

# Optional legacy Shiprocket tracking integration. It returns no tracking data
# unless both live credentials are configured.
SHIPROCKET_EMAIL=
SHIPROCKET_PASSWORD=
